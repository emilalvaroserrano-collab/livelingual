# Required for the live meeting translator.
# Add the real value in Vercel -> Project Settings -> Environment Variables.
GEMINI_API_KEY=

# Optional. Leave unset unless the app is using an external Postgres database.
# DATABASE_URL=

# This workspace currently runs without authentication.
VITE_AUTH_ENABLED=false
